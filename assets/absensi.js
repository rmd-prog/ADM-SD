/* GURU+ SD — ABSENSI V1 */
(function(){
'use strict';
const API=(window.API_BASE||window.WORKER_URL||'').replace(/\/$/,'');
const STAT=['Hadir','Sakit','Izin','Alpa'];
const esc=s=>String(s??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]));
function token(){return localStorage.getItem('token')||localStorage.getItem('authToken')||localStorage.getItem('access_token')||''}
function teacher(){return window.currentTeacher||window.currentUser||{} }
function rombels(){return ['IA','IB','IIA','IIB','IIIA','IIIB','IVA','IVB','V','VI']}
async function api(path,opt={}){const h={'Content-Type':'application/json',...(opt.headers||{})};const t=token();if(t)h.Authorization='Bearer '+t;const r=await fetch(API+'/api/absensi'+path,{...opt,headers:h});const j=await r.json().catch(()=>({ok:false,message:'Respons server tidak valid.'}));if(!r.ok||j.ok===false)throw new Error(j.message||'Gagal memproses absensi.');return j}
function today(){return new Date().toISOString().slice(0,10)}
function addUI(){
 const side=document.querySelector('.side');const main=document.querySelector('.main');if(!side||!main||document.getElementById('absensi'))return false;
 if(!document.getElementById('navAbsensi')){const b=document.createElement('button');b.id='navAbsensi';b.className='navbtn';b.innerHTML='📋 Absensi';b.onclick=open;side.appendChild(b)}
 return true
}
function open(){document.querySelectorAll('.page').forEach(x=>x.classList.remove('active'));const p=document.getElementById('absensi');if(p)p.classList.add('active');document.querySelectorAll('.navbtn').forEach(x=>x.classList.remove('active'));const n=document.getElementById('navAbsensi');if(n)n.classList.add('active');load();}
async function load(){const p=document.getElementById('absensi');if(!p)return;const r=teacher();const gm=r.role==='guru_mapel';const own=String(r.rombel||r.kelas||'');p.innerHTML=`<div class="card panel"><div class="student-head"><div><h2>📋 Absensi Harian</h2><p class="muted">Catat dan kelola kehadiran siswa.</p></div><span class="badge">${gm?'Guru Mapel • Semua Rombel':'Guru Kelas • Rombel '+esc(own)}</span></div><div class="toolbar"><label style="min-width:180px;margin:0">Tanggal<input id="absDate" type="date" value="${today()}"></label><label style="min-width:180px;margin:0">Rombel<select id="absRombel">${rombels().map(x=>`<option value="${x}" ${(!gm&&x===own)?'selected':''}>${x}</option>`).join('')}</select></label><button class="btn secondary" id="absAll">✓ Hadir Semua</button><button class="btn primary" id="absSave">💾 Simpan Absensi</button></div><div id="absMsg" class="alert"></div><div class="tablewrap"><table class="table" style="min-width:760px"><thead><tr><th>No</th><th>Nama Siswa</th><th>Status</th><th>Catatan</th></tr></thead><tbody id="absBody"><tr><td colspan="4">Memuat data siswa...</td></tr></tbody></table></div></div><div class="card panel"><h3>📊 Rekap Bulanan</h3><div class="toolbar"><input id="absMonth" type="month" value="${today().slice(0,7)}"><button class="btn secondary" id="absRecap">Tampilkan Rekap</button><button class="btn primary" id="absPrint">🖨️ Cetak</button></div><div class="tablewrap"><table class="table" style="min-width:700px"><thead><tr><th>No</th><th>Nama</th><th>H</th><th>S</th><th>I</th><th>A</th><th>Total</th></tr></thead><tbody id="absRecapBody"><tr><td colspan="7">Pilih Tampilkan Rekap.</td></tr></tbody></table></div></div>`;
 const sel=document.getElementById('absRombel');if(!gm){sel.disabled=true;sel.value=own}
 document.getElementById('absDate').onchange=loadRows;sel.onchange=loadRows;document.getElementById('absAll').onclick=()=>document.querySelectorAll('.absStatus').forEach(x=>x.value='Hadir');document.getElementById('absSave').onclick=save;document.getElementById('absRecap').onclick=recap;document.getElementById('absPrint').onclick=printRecap;loadRows();}
async function loadRows(){const body=document.getElementById('absBody');if(!body)return;try{const d=document.getElementById('absDate').value,r=document.getElementById('absRombel').value;j=await api(`?rombel=${encodeURIComponent(r)}&tanggal=${encodeURIComponent(d)}`);const siswa=j.siswa||[];const old=new Map((j.data||[]).map(x=>[String(x.siswa_id),x]));body.innerHTML=siswa.length?siswa.map((s,i)=>{const id=String(s.id),a=old.get(id)||{};return `<tr data-id="${esc(id)}"><td>${i+1}</td><td><b>${esc(s.nama)}</b><div class="muted">${esc(s.nis||s.nisn||'')}</div></td><td><select class="absStatus">${STAT.map(x=>`<option ${x===(a.status||'Hadir')?'selected':''}>${x}</option>`).join('')}</select></td><td><input class="absNote" value="${esc(a.catatan||'')}" placeholder="Catatan opsional"></td></tr>`}).join(''):'<tr><td colspan="4">Belum ada siswa pada rombel ini.</td></tr>'}catch(e){body.innerHTML=`<tr><td colspan="4" class="danger-text">${esc(e.message)}</td></tr>`}}
async function save(){const d=document.getElementById('absDate').value,r=document.getElementById('absRombel').value,rows=[...document.querySelectorAll('#absBody tr[data-id]')];const msg=document.getElementById('absMsg');try{const items=rows.map(x=>({siswa_id:Number(x.dataset.id),rombel:r,tanggal:d,status:x.querySelector('.absStatus').value,catatan:x.querySelector('.absNote').value}));await api('',{method:'POST',body:JSON.stringify({items})});msg.textContent='Absensi berhasil disimpan.';msg.className='alert successbox';msg.style.display='block';setTimeout(()=>msg.style.display='none',2500)}catch(e){msg.textContent=e.message;msg.className='alert';msg.style.display='block'}}
async function recap(){const body=document.getElementById('absRecapBody');try{const r=document.getElementById('absRombel').value,m=document.getElementById('absMonth').value,j=await api(`?rombel=${encodeURIComponent(r)}&bulan=${encodeURIComponent(m)}`);body.innerHTML=(j.data||[]).map((x,i)=>`<tr><td>${i+1}</td><td>${esc(x.nama_siswa)}</td><td>${x.H||0}</td><td>${x.S||0}</td><td>${x.I||0}</td><td>${x.A||0}</td><td>${x.total||0}</td></tr>`).join('')||'<tr><td colspan="7">Belum ada data.</td></tr>'}catch(e){body.innerHTML=`<tr><td colspan="7" class="danger-text">${esc(e.message)}</td></tr>`}}
function printRecap(){const box=document.getElementById('absRecapBody');if(!box||!box.innerHTML)return;const w=window.open('','_blank');w.document.write(`<html><head><title>Rekap Absensi</title><style>body{font-family:Arial;padding:20px}table{width:100%;border-collapse:collapse}th,td{border:1px solid #333;padding:7px}h2{text-align:center}@page{size:A4;margin:12mm}</style></head><body><h2>REKAPITULASI KEHADIRAN SISWA</h2><p>Rombel: ${esc(document.getElementById('absRombel').value)} &nbsp; Bulan: ${esc(document.getElementById('absMonth').value)}</p><table><thead><tr><th>No</th><th>Nama</th><th>H</th><th>S</th><th>I</th><th>A</th><th>Total</th></tr></thead><tbody>${box.innerHTML}</tbody></table><script>window.print();</script></body></html>`);w.document.close()}
function boot(){let tries=0;const t=setInterval(()=>{if(addUI()||++tries>30)clearInterval(t)},500);const obs=new MutationObserver(addUI);obs.observe(document.body,{childList:true,subtree:true})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();
